# Incluído no mesmo bloco server de nginx.conf: reutiliza os limites, os headers
# e os locations existentes. Certificado/chave são gerados localmente, fora do Git.
listen 443 ssl;
ssl_certificate /etc/nginx/certs/localhost.pem;
ssl_certificate_key /etc/nginx/certs/localhost.key;
